[project]
name = "traden"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Akash", email = "akashrbllps.1444@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5",
    "pydantic-settings>=2.15.0",
    "sqlalchemy>=2.0.54",
]

[project.scripts]
traden = "traden:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
